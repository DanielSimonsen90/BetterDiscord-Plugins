import { ReactDOM } from "@dium";

export const {
  createPortal,
  createRoot,
  preconnect,
  prefetchDNS,
  flushSync,
  preinit,
  preinitModule,
  preload,
  preloadModule,
  useFormStatus,
  requestFormReset,
  useFormState,
} = ReactDOM;

export { ReactDOM }