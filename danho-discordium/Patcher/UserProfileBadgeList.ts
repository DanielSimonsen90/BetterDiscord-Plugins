export type UserProfileBadgeList = {
    "aria-label": "User Badges",
    children: Array<React.ReactElement<{
        allowOverflow: boolean,
        children: JSX.Element,
        color: string,
        disableTooltipPoinerEvents: boolean,
        forceOpen: boolean,
        hideOnClick: boolean,
        position: "top" | "bottom",
        shouldShow: boolean,
        spacing: number,
        text: string,
    }>>,
    className: string,
    role: 'group'
}
export default UserProfileBadgeList;